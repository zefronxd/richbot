# Hasii Music Bot

## Running on Replit

- Install dependencies from `requirements.txt`.
- Start the bot with `bash start`.
- The configured Replit workflow is **Start bot** and runs `bash start` as a console service.
- Required environment variables are managed through Replit Secrets; do not commit a `.env` file.