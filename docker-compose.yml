version: '3.8'
services:
  hasii:
    build: .
    container_name: hasii_music_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./ZefronMusic/cookies:/app/ZefronMusic/cookies
      - ./downloads:/app/downloads
    command: bash start
